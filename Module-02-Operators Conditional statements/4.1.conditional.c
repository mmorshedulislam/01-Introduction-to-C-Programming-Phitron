#include <stdio.h>
int main()
{
    // conditional statements
    int tk;
    scanf("%d", &tk);

    if (tk >= 100) {
        printf("Burgar khabo");
    } else {
        printf("Burgar khabo na");
    }

    return 0;
}
