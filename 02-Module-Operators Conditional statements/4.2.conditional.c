#include <stdio.h>
int main()
{
    // conditional statements
    int tk = 550;
    // scanf("%d", &tk);

    if (tk >= 100) {
        printf("Burgar khabo");
    } 
    else if (tk >= 50) {
        printf("Fucka khabo");
    }
    else {
        printf("khabo na");
    }

    return 0;
}
